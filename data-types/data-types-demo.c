#include <stdio.h>
#include <stdlib.h>


int main() {

    int wholeNumber = 10; // whole numbers like 40 or 41
    double decimal = 3.7; // decimals like 40.0 or 40.5
    char characterSingle = 'a'; // only store one character
    char longString[] = "array of characters"; // uses array of characters for a long word

    printf("a letter is: %c", characterSingle);


    return 0;
}